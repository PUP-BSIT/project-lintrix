<?php
/*
include_once("database.php");

session_start();

if (isset($_SESSION['user'])) {
    $data = array('loggedIn' => true, 'user' => $_SESSION['user']);
    echo json_encode($data);
} else {
    $data = array('loggedIn' => false);
    echo json_encode($data);
}
*/
?>